---
# include task for testing
- hosts: vagrant
  tasks:
    - include_vars: ../vars/main.yml
    - include: ../tasks/main.yml

- hosts: localhost
  remote_user: root
  tasks:
    - include_vars: ../vars/main.yml
    - include: ../tasks/main.yml
