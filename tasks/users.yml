---

- name: ensure group exists
  group: name={{username}} state=present
  sudo: true

- name: ensure user exists
  user:
    name: "{{username}}"
    groups: "{{username}}"
    append: yes
  sudo: true

